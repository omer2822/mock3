{
  "name": "@live-poll/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "node --import tsx --test tests/polls.test.ts",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@live-poll/config": "0.1.0",
    "@types/express": "^5.0.1",
    "tsx": "^4.19.3"
  }
}
